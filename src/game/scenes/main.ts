import { Scene } from '@/game/utils'

export class MainScene extends Scene {
  constructor () {
    super('MainScene', {
      cols: 10,
      rows: 10
    })
  }

  create () {
    super.create()
  }
}
